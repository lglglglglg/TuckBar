# v0.8.9 收纳面板无闪现

## 问题

收纳篮首次打开或图标缓存失效时，旧逻辑会先调用 `expandHiddenSection()`，等待原始菜单栏图标出现后截图，再收起并显示聚合面板。用户因此会看到“原始图标先出现、随后进入收纳篮”的明显闪现。

## 修复

- 删除打开收纳篮时的展开与截图流程。
- 图标截图统一在应用隐藏布局时完成：此时原始项目本来就处于可见状态，截图不会产生额外闪现。
- 打开收纳篮只读取已有缓存，并在展示前保持隐藏边界收起。
- 缓存暂时不可用时宁可不展示该项，也不恢复原始菜单栏项目。
