# v0.7.4 图标尺寸与设置交互

- 聚合条以原窗口逻辑尺寸显示完整快照，取消 22pt 高度压缩。原 33pt 菜单栏截图保持 33pt，原图内部留白保持不变。
- 移除每个图标额外的 7pt 间隔；点击区域覆盖完整单元。
- 侧栏按钮及退出按钮增加矩形命中区域，空白处也可点击。
- 设置窗口缩小为 780×550，合并教程与关于为“帮助与关于”。
- 移除通用设置的重复打开按钮、重复数量展示及可能误导的“已展开”摘要，保留具体运行结果。
- 自动应用继续保留；手动入口命名为“重新整理”用于重试。菜单栏右键保留打开、临时展开、设置和退出。

本次不改动权限身份或重置系统授权。当前仍为临时签名，新构建可能需要重新授权。上一版 ZIP 保留在 dist。
