# v0.9.0 收纳面板竞态修复

## 问题

视频复现显示：鼠标进入 TuckBar 后，顶部原始菜单栏项目与下方收纳篮同时可见，随后原始项目才消失。原因是 `NSStatusItem.length` 的变化要等下一次状态栏布局周期生效，面板却在同一周期内立即显示。

## 修复

- 鼠标进入 TuckBar 触发器时立即重新设置收起边界。
- 面板显示前等待状态栏完成一次布局周期，再显示收纳篮。
- 打开过程中不再展开隐藏区，也不再使用原始项目作为截图来源。

这样即使锁屏唤醒后 macOS 暂时恢复了状态栏布局，也不会把原始项目和收纳篮同时展示给用户。
