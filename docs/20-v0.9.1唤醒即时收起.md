# v0.9.1 唤醒即时收起

## 问题

锁屏或睡眠唤醒时，旧恢复队列先调用 `expand()`，等待菜单栏服务稳定后才重新收起。用户因此会看到原始菜单栏图标持续一段时间。

## 修复

- 锁屏/睡眠前立即保持隐藏边界收起，并关闭聚合面板与悬停任务。
- 唤醒后立即调用 `collapse()`，不再先展开原始菜单栏。
- 唤醒验证阶段只重新扫描并再次收起边界，不执行会暴露原始图标的 Command 拖动重排。
- 只有显示器布局等需要真正重排的场景，才使用完整恢复流程。
